{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: VideoSearch",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/app.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "justMyCode": true,
            "stopOnEntry": false,
            "args": [],
            "pythonPath": "${command:python.interpreterPath}"
        }
    ]
}